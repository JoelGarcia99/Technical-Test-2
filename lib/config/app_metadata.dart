/// Contains metadata information about the app such as
/// the version number, app name, etc.
abstract class AppMetadata {

	/// App name to show in the app.
	static const String appName = 'Technical Test';
}
